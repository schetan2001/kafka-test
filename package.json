{
  "name": "postgres-to-kafka",
  "version": "1.0.0",
  "description": "Service to poll a PostgreSQL table and publish changes to a Kafka topic.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "kafkajs": "^2.2.4",
    "pg": "^8.11.3"
  }
}