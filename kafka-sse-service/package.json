{
  "name": "kafka-sse-service",
  "version": "1.0.0",
  "description": "Consumes JSON messages from Kafka and exposes them via a Server-Sent Events (SSE) REST API.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "kafkajs": "^2.2.4"
  }
}
